version: "2.4"

services:

  backend-proxy:
    build:
      context: ./
      dockerfile: containers/backend-proxy/Dockerfile
    environment:
      - "ENDPOINT_HOSTNAME"
      - "ENDPOINT_SSH_PORT"
    restart: always

  controller:
    build:
      context: ./
      dockerfile: containers/controller/Dockerfile
    restart: always
    devices:
      - "/dev/i2c-1"

